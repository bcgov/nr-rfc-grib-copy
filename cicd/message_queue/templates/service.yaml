apiVersion: v1
kind: Service
metadata:
  name: {{ include "message_queue.fullname" . }}
  labels:
    {{- include "message_queue.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "message_queue.selectorLabels" . | nindent 4 }}
